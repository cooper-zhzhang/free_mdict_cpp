# free_mdict_cpp

## 项目概述

free_mdict_cpp 是一个开源的 MDX 格式字典文件解析工具，支持字典信息显示、关键词列表输出和交互式单词查询功能。

## 使用方法

### 基本用法

```bash
./free_mdict [options] <dict_path> 
```

### 查看字典信息

```bash
./free_mdict /path/to/dictionary.mdx
```

### 列出所有关键词

```bash
./free_mdict --keys /path/to/dictionary.mdx
```

### 交互式查询模式

```bash
./free_mdict --search /path/to/dictionary.mdx
```

## 命令行参数说明

- `<dict_path>`: 字典文件路径（必需，作为位置参数提供）
- `--verbose`: 启用详细输出信息
- `--keys`: 列出所有关键词
- `--search`: 启动交互式查询模式
- `--help`: 显示帮助信息

## 性能特性

- 使用二分查找算法优化记录块的查询效率
- 支持内存管理，在小内存模式下可以释放不再使用的资源
- 提供字典初始化时间统计功能

## 开发与贡献

欢迎提交Issue和Pull Request来帮助改进这个项目。

## 依赖安装与编译

### 依赖库

项目依赖以下库：

- CMake (3.10或更高版本)
- zlib
- gflags (可选)

### 在macOS上安装依赖

使用Homebrew安装依赖：

```bash
brew install cmake zlib gflags
```

### 在Ubuntu/Debian上安装依赖

使用apt安装依赖：

```bash
sudo apt-get update
sudo apt-get install cmake libz-dev libgflags-dev
```

### 在CentOS/RHEL上安装依赖

使用yum安装依赖：

```bash
sudo yum install cmake zlib-devel gflags-devel
```

### 编译项目

1. 克隆仓库

```bash
git clone git@github.com:cooper-zhzhang/free_mdict_cpp.git
cd free_mdict_cpp
```

2. 创建构建目录

```bash
mkdir build
cd build
```

3. 配置项目（默认使用Release模式）

```bash
cmake ..
```

4. 编译项目

```bash
make -j$(nproc)  # 或使用 make -j$(sysctl -n hw.ncpu) (在macOS上)
```

5. 安装（可选）

```bash
make install
```

安装后的文件将位于：
- 可执行文件: `/usr/local/free_mdict/bin/free_mdict`
- 静态库: `/usr/local/free_mdict/lib/libfree_mdict.a`
- 头文件: `/usr/local/free_mdict/include/mdict_extern.h`

### 编译选项

可以通过CMake选项自定义编译行为：

```bash
# 启用小内存模式
cmake -DSMALL_MEMORY=ON ..
目前小内存模式下，查询速度可能会变慢，因为需要频繁分配和释放内存。
小内存模式下不支持获取所有关键词，只能查询单个关键词。

# 更改构建类型（Debug/Release）
cmake -DBUILD_TYPE=Debug ..
```

## 注意事项

- 当前版本主要支持MDX格式字典文件
- 当前仅支持version 2.0的字典文件
- 仅支持UTF-8/UTF-16编码的字典文件
- 仅支持zlib压缩的字典文件
- 请确保使用的字典文件格式正确且完整


## 静态库使用
安装静态库后，头文件`mdict_extern.h`位于`/usr/local/free_mdict/include/`目录下。
链接静态库时，需要添加`-lfree_mdict`选项。
